namespace Sjuan {
    //Only contains winner for now
    //Should give more yummy data later
    struct GameResult {
        public int winner;

        public GameResult(int winner) {
            this.winner = winner;
        }
    }
}